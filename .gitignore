# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel

# playwright
/test-results/
/playwright-report/
/blob-report/
/.playwright-mcp/
/.playwright-cli/

# local dev artifacts — source PDF, extraction outputs, Codex marker
.codex
/.claude/
*.pdf
!/public/psalter.pdf
/parsed_data/
/output_sample.txt
/parsed_raw.txt
/psalter_full_text.txt
# PDF plain-text dump — regenerated by `pdftotext -layout public/psalter.pdf
# public/psalter.txt` on demand. POC input for FR-153g (pilot 규격 재추출),
# not a deploy asset. public/ location is historical; moving to scripts/out/
# would break existing FR-153 worker references if any.
/public/psalter.txt

# personal operational checklists (not shared to repo)
/docs/security-incident-*.md
